import { StyleSheet } from "react-native";

const RecentGameListStyle = StyleSheet.create({
    recentGameList_container : {
        width : '100%',

        marginLeft : 30,
        marginTop : 10,
    },

    recentGameList_title : {
        fontSize : 18,
        fontWeight : 'bold',

        marginBottom : 10,
    },
});

export default RecentGameListStyle;