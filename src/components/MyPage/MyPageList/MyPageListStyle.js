import { StyleSheet } from "react-native";

const MyPageListStyle = StyleSheet.create({
    mypagelist_container : {
        width : '100%'
    },

    mypagelist_title : {
        width : '100%',
        
        alignItems : 'left',

        margin : 30,

        fontSize : 20,
        fontWeight : 'bold',
    }
});

export default MyPageListStyle;